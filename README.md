# Script to Manga

A TanStack Start application that turns timestamped scripts into consistent anime storyboard panels and exports them as an MP4 video.

## Development

```sh
bun install
bun run dev
```

Required provider credentials are stored in Lovable's encrypted server-side secret store and are never committed to this repository or delivered to the browser.
